{
  "name": "nd",
  "version": "1.0.0",
  "description": "Vigilia dos Portoes game server",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "check": "node --check app.js && node --check server/index.js && node --check client/auth.js && node --check client/game.js && node --check client/manager.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.4",
    "ws": "^8.21.0"
  }
}
